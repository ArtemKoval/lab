You repair one JavaScript function.
Output ONLY the corrected function as raw JavaScript. No markdown code fences. No prose. No explanation. No comments.
Start the output with the exact text "export function ". Keep the name, the parameter names and the parameter order that the contract gives.
Obey every rule of the contract, including the rules that already pass. Do not delete a guard that is already correct.
Return the literal true or the literal false when the contract asks for a boolean. Never return 1 or 0 for a boolean.
Never compare two objects with === or with Array.prototype.includes. Compare the x number and the y number.
Use integer arithmetic only. Do not add a second export.
